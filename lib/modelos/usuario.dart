class Usuario {
  int id;
  String usuario;
  String nombre;
  String password;
  String estadoCivil;
  String escolaridad;
  String habilidades;

  Usuario(this.id, this.usuario, this.nombre, this.password, this.estadoCivil,
      this.escolaridad, this.habilidades);
}
